{
  "moduleId": "secrets",
  "mode": "credential-safety",
  "allowedActions": [
    "Store credentials in the encrypted vault.",
    "Reference credentials by logical secret name from service configs.",
    "Reveal secret values only when explicitly requested."
  ],
  "forbiddenActions": [
    "Write plaintext credentials to config files.",
    "Print secret values during list or set operations.",
    "Include secret values in usage logs."
  ],
  "approvalRequired": true
}
