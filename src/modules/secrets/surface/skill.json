{
  "moduleId": "secrets",
  "purpose": "Manage encrypted local credentials used by external service integrations.",
  "whenToUse": [
    "The user needs to store credentials for an external service.",
    "The user needs to list configured secret references.",
    "The user needs to reveal or remove a stored credential."
  ],
  "whenNotToUse": [
    "The user wants to store non-sensitive public configuration.",
    "The user wants project-local state unrelated to external service credentials."
  ]
}
