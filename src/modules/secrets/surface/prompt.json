{
  "moduleId": "secrets",
  "templates": [
    {
      "id": "secrets.vault",
      "template": "Explain this Agent DevKit secret vault operation without exposing secret values unless reveal was explicitly requested: {{operation}}",
      "variables": ["operation"]
    }
  ]
}
