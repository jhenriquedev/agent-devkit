{
  "moduleId": "secrets",
  "summary": "Secrets are encrypted locally and stored under ~/.agent-devkit/secrets. Config files should reference secret names instead of embedding credentials.",
  "facts": [
    "The local vault file is ~/.agent-devkit/secrets/vault.json.",
    "The local key file is ~/.agent-devkit/keys/local.key.",
    "Secrets use AES-256-GCM envelopes before persistence.",
    "CLI usage logs must redact secret values passed to secrets commands."
  ]
}
