# user.personalization

Stores the local agent identity profile used by CLI, TUI, MCP and future agent loops.

The first profile version mirrors the onboarding identity step:

- character preset
- agent display name
- behavior
- tone
- detail level
- traits

Provider, mini-brain and memory settings stay in their own future capabilities.
