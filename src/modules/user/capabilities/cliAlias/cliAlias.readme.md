# user.cliAlias

Creates and manages a local command alias for the Agent DevKit CLI.

The official `agent` command remains available. The alias is a managed executable shim under `~/.agent-devkit/bin/<alias>` that forwards arguments to `agent`.
