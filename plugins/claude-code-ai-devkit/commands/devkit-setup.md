# devkit-setup

Run:

```bash
python3 plugins/claude-code-ai-devkit/scripts/bootstrap.py
```

Use this to verify the local Agent DevKit runtime path. Do not ask for provider
credentials during setup.
