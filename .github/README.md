# GitHub Automation

This directory contains CI and npm release workflows for Agent DevKit.

The rewrite publishes the package directly from the repository root. Legacy
release steps that built a nested package runtime are intentionally not reused.
