id: topdesk-orchestrator.create-incident
kind: capability
name: Create Incident
version: 0.1.0
status: draft
purpose: >
  Criar incidente TOPdesk com dry-run por padrao.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  prompt: ../../knowledge/prompts/create-incident.md
  output_template: ../../templates/create-incident-output.md
inputs:
  required: [brief_description, request]
  optional: [caller_id, category, priority, operator_group, fields_json, execute, fixture, output]
outputs:
  artifacts: [incident-create-plan.md]
integration:
  repository: ../../infra/integrations/topdesk/topdesk_repository.py
  methods:
    - ../../infra/integrations/topdesk/methods/create-incident.yaml
write_policy: confirm
