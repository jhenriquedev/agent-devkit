# Workflow

1. Receber `agent_id`.
2. Conferir estrutura obrigatoria do agente.
3. Conferir capabilities declaradas e presentes em disco.
4. Conferir `workflow.md`, `decision-rules.md` e `write_policy`.
5. Retornar erros e warnings estruturados.
