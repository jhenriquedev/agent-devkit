# Decision Rules

- Nunca escrever arquivos nesta capability.
- Bloquear `agent_id` que nao seja kebab-case.
- Bloquear tentativa de path traversal.
- Exigir pelo menos uma capability.
- Sinalizar perguntas pendentes em vez de inventar contrato ausente.
