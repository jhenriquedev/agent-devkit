id: extract-contract
description: Extract a normalized technical integration contract.
inputs:
  required: [sources]
  optional: [base_url]
outputs:
  fields: [protocols, primary_protocol, base_url, auth, operations, errors, missing_information, flow]
read_only: true
