# Workflow

1. Validar spec com planejamento PyAutoGUI.
2. Validar `agent-id` e `capability-id` em kebab-case.
3. Exigir `target_project`.
4. Em dry-run, retornar arquivos planejados.
5. Com `--execute`, escrever capability apenas dentro do projeto alvo.
