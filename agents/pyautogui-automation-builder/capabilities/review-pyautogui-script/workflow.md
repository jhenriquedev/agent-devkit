# Workflow

1. Ler script informado por `--input` ou `--text`.
2. Verificar failsafe, dry-run, confirmacao, screenshots e abort.
3. Apontar uso inseguro de coordenadas absolutas, segredo hardcoded ou falta de
   validacao visual.
4. Retornar JSON read-only com findings.
