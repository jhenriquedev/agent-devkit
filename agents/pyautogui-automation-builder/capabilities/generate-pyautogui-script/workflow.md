# Workflow

1. Reutilizar o planejamento da spec.
2. Gerar script sem escrever arquivo local.
3. Manter dry-run como comportamento padrao.
4. Exigir `--execute --confirm` para acao real.
5. Incluir screenshots, failsafe, abort file, timeout e logs.
