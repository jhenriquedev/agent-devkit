# Decision Rules: Generate Log Report

- Separate counts and samples as fato from inferred patterns as inferencia.
- State the exact source and janela `from`/`to`.
- Mention if the result `limit` was reached and the report may be truncated.
- Do not recommend operational action without linking it to observed evidence.
