# Infra

Infra contem o suporte executavel do agente Elasticsearch Log Analyzer.

Use esta camada apenas para repositories, models, contracts e CLIs de
integracao. Regras de decisao e apresentacao pertencem a `capabilities/`,
`knowledge/` e `templates/`.
