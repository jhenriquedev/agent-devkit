id: excel-workbook-builder.refresh-workbook-data
kind: capability
name: Refresh Workbook Data
version: 0.1.0
status: draft
purpose: Refresh workbook data while preserving formulas and layout.
entrypoint:
  runner: runner.py
  workflow: workflow.md
inputs:
  required: [workbook, input]
outputs:
  artifacts: [workbook.xlsx]
write_policy: output_only
