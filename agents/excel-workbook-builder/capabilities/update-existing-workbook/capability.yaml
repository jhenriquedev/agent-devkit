id: excel-workbook-builder.update-existing-workbook
kind: capability
name: Update Existing Workbook
version: 0.1.0
status: draft
purpose: Update an existing workbook while preserving structure and formatting.
entrypoint:
  runner: runner.py
  workflow: workflow.md
inputs:
  required: [workbook, input]
outputs:
  artifacts: [workbook.xlsx]
write_policy: output_only
