id: excel-workbook-builder.create-pivot-summary
kind: capability
name: Create Pivot Summary
version: 0.1.0
status: draft
purpose: Create grouped/pivot-style summaries from source data.
entrypoint:
  runner: runner.py
  workflow: workflow.md
inputs:
  required: [input, group_by]
  optional: [values]
outputs:
  artifacts: [workbook.xlsx]
write_policy: output_only
