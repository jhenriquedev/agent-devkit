# Decision Rules

- Nao escrever arquivos.
- Suportar inicialmente apenas Python e Node.js.
- Deploy real fica fora do escopo.
- IAM deve ser minimo ou justificado.
- Secrets devem ser referencias, nunca valores hardcoded.
