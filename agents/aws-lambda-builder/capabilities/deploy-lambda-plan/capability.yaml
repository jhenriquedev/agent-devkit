id: aws-lambda-builder.deploy-lambda-plan
kind: capability
name: Deploy Lambda Plan
version: 0.1.0
status: draft
purpose: Gerar plano de deploy AWS Lambda sem aplicar mudancas.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  output_template: ../../templates/lambda-deploy-plan.md
inputs:
  required:
    - spec
  optional: []
outputs:
  artifacts:
    - lambda-deploy-plan.json
write_policy: dry_run
