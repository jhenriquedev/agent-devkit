id: aws-lambda-builder.review-lambda-security
kind: capability
name: Review Lambda Security
version: 0.1.0
status: draft
purpose: Revisar spec Lambda contra IAM, secrets, env vars e operacao segura.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  output_template: ../../templates/lambda-security-review.md
inputs:
  required:
    - spec
  optional: []
outputs:
  artifacts:
    - lambda-security-review.json
write_policy: read_only
