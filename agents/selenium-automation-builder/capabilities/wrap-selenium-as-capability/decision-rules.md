# Decision Rules

- Wrapper nao executa browser durante geracao.
- Escrita fica limitada ao `target_project`.
- `agent-id` e `capability-id` devem ser kebab-case.
- Write policy da capability embrulhada deve refletir side effects.
