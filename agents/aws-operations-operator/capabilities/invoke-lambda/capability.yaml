id: aws-operations-operator.invoke-lambda
kind: capability
name: Invoke Lambda
version: 0.1.0
status: draft
purpose: Invocar Lambda com payload opcional e dry-run por padrao.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  prompt: ../../knowledge/prompts/invoke-lambda.md
  output_template: ../../templates/operation-plan-output.md
inputs:
  required: [function_name, environment]
  optional: [payload, profile, region, execute, confirm_resource, output_dir, yes_create_dir, yes_overwrite]
outputs:
  artifacts: [operation-plan.md, operation-dry-run.json, operation-result.json, rollback-notes.md]
integration:
  repository: ../../infra/integrations/aws-operations/aws_operations_repository.py
  methods:
    - ../../infra/integrations/aws-operations/methods/invoke-lambda.yaml
write_policy: confirm
