# Regras

- Inspecionar arquivo Figma existente antes de propor qualquer update.
- Em direct mode, exigir evidencia real como `file_key`, `file_url` ou `inspected_node_ids`.
- Sem bridge/permissao, operar em `plan_only` e gerar `figma-action-plan.md`.
- Mapear paginas, frames, componentes, variaveis, estilos e oportunidades de reuso.
- Preferir criar nova pagina/frame ou versao para designs validados.
- Nao afirmar inspecao real sem resultado retornado pelo bridge.
