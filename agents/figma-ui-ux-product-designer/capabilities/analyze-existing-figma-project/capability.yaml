id: figma-ui-ux-product-designer.analyze-existing-figma-project
kind: capability
name: Analyze Existing Figma Project
version: 0.1.0
status: draft
purpose: Inspecionar projeto Figma existente, mapear paginas, frames, design system e oportunidades de reuso.
entrypoint:
  runner: runner.py
  workflow: workflow.md
inputs:
  optional: [figma_file_url, source, require_direct, output_dir]
outputs:
  artifacts: [figma-action-plan.md, design-brief.md, screen-inventory.md]
integration:
  provider: figma
  mode: direct_mcp_or_plan_only
write_policy: read_only
