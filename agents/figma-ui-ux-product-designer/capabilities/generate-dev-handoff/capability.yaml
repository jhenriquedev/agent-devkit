id: figma-ui-ux-product-designer.generate-dev-handoff
kind: capability
name: Generate Dev Handoff
version: 0.1.0
status: draft
purpose: Gerar handoff de design para desenvolvimento com telas, estados, componentes, tokens e decisoes abertas.
entrypoint:
  runner: runner.py
  workflow: workflow.md
inputs:
  optional: [brief, source, figma_file_url, output_dir]
outputs:
  artifacts: [dev-handoff.md, design-brief.md, source-traceability.md]
write_policy: ask_before_creating_output_directory
