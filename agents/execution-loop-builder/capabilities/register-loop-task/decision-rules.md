# Decision Rules

- Registro real requer `--execute`.
- Side effects externos devem ser refletidos em `action.external_writes`.
- Scheduler nao recebe permissao de escrita externa automaticamente.
- Trigger manual nao deve ser agendado automaticamente.
