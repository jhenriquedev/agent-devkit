# Decision Rules

- Configurar apenas canais locais suportados.
- Nao persistir segredos nem tokens.
- `local` deve ser tratado como alias de `desktop`.
- Filtros de evento devem usar somente eventos suportados pelo runtime.
