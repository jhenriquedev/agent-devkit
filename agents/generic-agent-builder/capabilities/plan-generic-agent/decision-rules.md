# Decision Rules

- Retornar `needs-input` quando faltar objetivo, nome ou contexto de dominio.
- Bloquear `target_host` fora da lista suportada.
- Bloquear segredos, tokens, URLs e marcadores sensiveis.
- Planejar escrita apenas como artefato revisavel.
