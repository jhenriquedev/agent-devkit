# Workflow

1. Ler e validar a spec.
2. Exigir `target_project` para qualquer plano de escrita.
3. Retornar dry-run por padrao.
4. Escrever somente quando `--execute` for informado e o runtime permitir.
5. Bloquear sobrescrita sem `--allow-overwrite`.
