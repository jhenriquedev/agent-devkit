id: detect-sensitive-columns
description: Detect likely sensitive columns by metadata.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
