id: run-readonly-query
description: Execute a read-only SELECT/WITH query with automatic TOP limit and PII masking.
inputs:
  required: [query]
  optional: [database, limit, format]
outputs:
  fields: [row_count, limit, rows]
read_only: true
