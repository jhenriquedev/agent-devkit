id: explain-query-plan
description: Return the estimated execution plan (SHOWPLAN_TEXT) for a query without executing it.
inputs:
  required: [query]
  optional: [database, format]
outputs:
  fields: [query, plan]
read_only: true
