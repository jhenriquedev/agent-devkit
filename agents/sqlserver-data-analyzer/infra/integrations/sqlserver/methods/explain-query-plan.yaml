id: explain-query-plan
description: Generate and summarize a safe estimated query plan.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
