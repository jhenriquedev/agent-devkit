id: sample-table
description: Return a masked TOP N sample of rows from a table.
inputs:
  required: [schema, table]
  optional: [database, limit, format]
outputs:
  fields: [schema, table, row_count, rows]
read_only: true
