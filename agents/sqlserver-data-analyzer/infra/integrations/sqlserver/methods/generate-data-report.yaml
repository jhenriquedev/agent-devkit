id: generate-data-report
description: Generate a SQL Server data analysis report.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
