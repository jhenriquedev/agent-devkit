id: compare-tables
description: Compare structure and basic samples between two tables.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
