id: compare-tables
description: Compare structure between two tables (columns, types).
inputs:
  required: [left-schema, left-table, right-schema, right-table]
  optional: [database, format]
outputs:
  fields: [common_columns, left_only_columns, right_only_columns]
read_only: true
