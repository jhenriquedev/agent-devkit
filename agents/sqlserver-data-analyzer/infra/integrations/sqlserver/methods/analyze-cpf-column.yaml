id: analyze-cpf-column
description: Analyze CPF format, check digits, blanks, and duplicates.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
