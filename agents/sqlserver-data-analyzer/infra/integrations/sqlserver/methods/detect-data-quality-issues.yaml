id: detect-data-quality-issues
description: Detect null, duplicate, format, and relationship quality issues.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
