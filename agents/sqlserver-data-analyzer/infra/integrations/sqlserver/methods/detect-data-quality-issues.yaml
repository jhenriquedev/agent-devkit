id: detect-data-quality-issues
description: Detect all_null and constant_value columns in a table.
inputs:
  required: [schema, table]
  optional: [database, format]
outputs:
  fields: [schema, table, issues]
read_only: true
