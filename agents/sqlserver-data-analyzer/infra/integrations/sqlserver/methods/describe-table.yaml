id: describe-table
description: Describe columns, keys, constraints, and indexes for a table.
inputs:
  required: [schema, table]
  optional: [database, format]
outputs:
  fields: [schema, table, columns, indexes, constraints]
read_only: true
