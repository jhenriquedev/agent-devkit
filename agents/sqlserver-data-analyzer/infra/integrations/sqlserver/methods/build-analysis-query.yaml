id: build-analysis-query
description: Build a safe SELECT query from schema, table, and columns. The --question argument is documented but requires host-side NL-to-parameter translation.
inputs:
  required: [schema, table]
  optional: [columns, limit, question, database, format]
outputs:
  fields: [query, notes]
read_only: true
