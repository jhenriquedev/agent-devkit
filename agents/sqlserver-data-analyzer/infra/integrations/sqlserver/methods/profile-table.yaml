id: profile-table
description: Profile each column of a table (null count, distinct count) without exposing individual data.
inputs:
  required: [schema, table]
  optional: [database, format]
outputs:
  fields: [schema, table, row_count, columns]
read_only: true
