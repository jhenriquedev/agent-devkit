id: profile-table
description: Profile row counts, nulls, distinct counts, and simple stats.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
