id: validate-readonly-query
description: Validate a query for read-only safety and inject TOP limit without executing.
inputs:
  required: [query]
  optional: [limit, format]
outputs:
  fields: [valid, query, limit]
read_only: true
