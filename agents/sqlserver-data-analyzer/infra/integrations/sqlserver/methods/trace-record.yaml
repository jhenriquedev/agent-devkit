id: trace-record
description: Trace a specific record and its FK relationships.
inputs:
  required: [schema, table, key-column, key-value]
  optional: [database, format]
outputs:
  fields: [base_rows, relationships]
read_only: true
