# Decision Rules: Correlate Azure Card Logs

- `azure_project`, `work_item_id` e `region` identificam o caso.
- Para AWS real, `log_group`, `start_time` e `end_time` tambem devem existir.
- Separar evidencias do Azure e do CloudWatch.
- Esta capability e read-only.
