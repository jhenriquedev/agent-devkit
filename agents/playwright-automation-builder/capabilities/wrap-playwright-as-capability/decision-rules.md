# Decision Rules

- Wrapper deve respeitar write policy derivada de side effects.
- Escrita fica limitada a `target_project`.
- Overwrite exige `--allow-overwrite`.
- Runner gerado deve manter dry-run, confirmacao, screenshots e traces.
