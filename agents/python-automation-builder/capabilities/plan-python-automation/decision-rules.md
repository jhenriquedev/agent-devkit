# Decision Rules

- Retornar `needs-input` quando faltar contrato minimo.
- Bloquear Selenium, PyAutoGUI e Playwright neste agente.
- Bloquear segredo, token ou URL na spec.
- Preferir standard library e apontar dependencias externas como risco.
