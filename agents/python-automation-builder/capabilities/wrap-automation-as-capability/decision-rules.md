# Decision Rules

- Wrapper nao executa a automacao gerada.
- Escrita fica limitada ao `target_project`.
- `agent-id` e `capability-id` devem ser kebab-case.
- A write policy da capability embrulhada deve refletir os side effects.
