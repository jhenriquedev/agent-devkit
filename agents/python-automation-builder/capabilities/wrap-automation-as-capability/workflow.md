# Workflow

1. Ler e validar a spec da automacao.
2. Exigir `target_project`, `agent-id` e `capability-id`.
3. Planejar arquivos de capability em dry-run.
4. Escrever apenas com `--execute` e confirmacao do runtime.
