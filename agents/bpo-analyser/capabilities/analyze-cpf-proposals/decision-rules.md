# Regras

- Proposta elegivel segue regra do Core: situacao `INT` ou `APR`, tipo `3` e
  limite de saque maior que zero.
- Propostas `CAD`, `PEN` ou `AND` indicam analise em andamento.
- Propostas `REP` indicam reprova.
