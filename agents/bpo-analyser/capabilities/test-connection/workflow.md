# Workflow

1. Carregar configuracao BPO do ambiente ou `.env`.
2. Validar presenca de URLs e credenciais obrigatorias.
3. Quando `--network` for usado, consultar os WSDLs dos endpoints.
4. Renderizar diagnostico sem expor segredos.
