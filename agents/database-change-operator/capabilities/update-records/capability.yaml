id: database-change-operator.update-records
kind: capability
name: Update Records
version: 0.1.0
status: draft
purpose: >
  Update records using explicit set JSON and where clause with dry-run by default.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  prompt: ../../knowledge/prompts/update-records.md
  decision_rules: decision-rules.md
  output_template: ../../templates/update-records-output.md
inputs:
  required: [schema, table, set_json, where]
  optional: [database, execute, fixture, output]
outputs:
  artifacts: [update-records-result.md]
integration:
  repository: ../../infra/integrations/postgres/database_change_repository.py
  methods:
    - ../../infra/integrations/postgres/methods/update-records.yaml
write_policy: confirm
