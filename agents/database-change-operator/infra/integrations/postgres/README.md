# Postgres Change Integration

Repository de escrita controlada para PostgreSQL usando `psql`.

As escritas reais exigem `--execute` na capability. O repository oferece dry-run
e validacao de seguranca antes da execucao.
