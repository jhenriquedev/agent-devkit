# Prompt

Valide se a string de conexao PostgreSQL permite escrita. Primeiro execute em dry-run. Execute com `--execute` apenas quando o usuario pedir validacao real. Informe que o teste real usa tabela temporaria e rollback.
