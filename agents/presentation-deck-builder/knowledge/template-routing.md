# Template Routing

1. Se `template-id` e `template-version` forem informados, usar essa versao.
2. Se apenas `template-id` for informado, usar `current_version`.
3. Se nao houver template, conduzir criacao ou solicitar template ao usuario.
4. Se a entrada estiver incompleta para o template, perguntar antes de gerar.
