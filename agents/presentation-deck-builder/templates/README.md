# Templates

Esta pasta armazena templates versionados do `presentation-deck-builder`.

Cada template deve ficar em `templates/<template-id>/` com manifest proprio e
subpastas de versao em `versions/<version>/`.
