# Workflow

1. Ler migrations, seed e Edge Functions.
2. Detectar `raw_user_meta_data`, `user_metadata`, service role e chaves.
3. Detectar uso inseguro de JWT/claims em autorizacao.
4. Retornar findings read-only.
