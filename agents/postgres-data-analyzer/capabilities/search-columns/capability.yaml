id: postgres-data-analyzer.search-columns
kind: capability
name: Search Columns
version: 0.1.0
status: draft
purpose: >
  Search columns by name, type, or sensitivity pattern.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  prompt: ../../knowledge/prompts/search-columns.md
  output_template: ../../templates/search-columns-output.md
inputs:
  required: []
  optional: [database, schema, table, left_schema, left_table, right_schema, right_table, column, columns, query, pattern, key_column, key_value, limit, fixture, output]
outputs:
  artifacts: [search-columns-output.md]
integration:
  repository: ../../infra/integrations/postgres/postgres_repository.py
  methods:
    - ../../infra/integrations/postgres/methods/search-columns.yaml
write_policy: read_only
