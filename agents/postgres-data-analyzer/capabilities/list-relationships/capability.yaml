id: postgres-data-analyzer.list-relationships
kind: capability
name: List Relationships
version: 0.1.0
status: draft
purpose: >
  List PK/FK relationships between PostgreSQL tables.
entrypoint:
  runner: runner.py
  workflow: workflow.md
  prompt: ../../knowledge/prompts/list-relationships.md
  output_template: ../../templates/list-relationships-output.md
inputs:
  required: []
  optional: [database, schema, table, left_schema, left_table, right_schema, right_table, column, columns, query, pattern, key_column, key_value, limit, fixture, output]
outputs:
  artifacts: [list-relationships-output.md]
integration:
  repository: ../../infra/integrations/postgres/postgres_repository.py
  methods:
    - ../../infra/integrations/postgres/methods/list-relationships.yaml
write_policy: read_only
