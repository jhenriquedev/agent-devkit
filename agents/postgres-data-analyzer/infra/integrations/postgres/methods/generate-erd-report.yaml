id: generate-erd-report
description: Generate a Markdown ERD-style relationship report.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
