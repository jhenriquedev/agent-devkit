id: compare-tables
description: Compare structure between two PostgreSQL tables.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
