id: estimate-table-size
description: Estimate row count and storage usage for tables.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
