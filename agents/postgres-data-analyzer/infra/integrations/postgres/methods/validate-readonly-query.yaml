id: validate-readonly-query
description: Validate whether a SQL query is safe and read-only.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
