id: list-relationships
description: List PK/FK relationships between PostgreSQL tables.
inputs:
  required: []
  optional: [database, schema, table, column, query, pattern, limit]
outputs:
  fields: [result]
read_only: true
