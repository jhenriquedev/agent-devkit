id: profile-table
description: Profile row count, null count, and distinct count for table columns.
inputs:
  required: [schema, table]
  optional: [database, limit_columns]
outputs:
  fields: [row_count, columns]
read_only: true
