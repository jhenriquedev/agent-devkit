id: backup-records
description: Create a logical backup of records selected by a where clause.
inputs:
  required: [schema, table, where]
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
