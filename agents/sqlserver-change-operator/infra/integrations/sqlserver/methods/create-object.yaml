id: create-object
description: Create SQL Server objects from reviewed SQL with safety checks.
inputs:
  required: [path]
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
