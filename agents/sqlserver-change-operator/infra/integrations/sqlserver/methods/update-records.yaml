id: update-records
description: Update records using explicit set JSON and where clause with dry-run by default.
inputs:
  required: [schema, table, set_json, where]
  optional: [database, execute, max_affected_rows]
outputs:
  fields: [result]
write_policy: confirm
