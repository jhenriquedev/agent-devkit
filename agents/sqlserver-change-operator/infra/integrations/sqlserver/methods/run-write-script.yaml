id: run-write-script
description: Execute a controlled T-SQL write script with dry-run by default.
inputs:
  required: [path]
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
