id: apply-migration
description: Apply one SQL Server migration with history tracking and --execute confirmation.
inputs:
  required: [path]
  optional: [database, rollback_path, name, execute]
outputs:
  fields: [result]
write_policy: confirm
