id: upsert-records
description: Upsert JSON or CSV records into a table with dry-run by default.
inputs:
  required: [schema, table, key_column, input]
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
