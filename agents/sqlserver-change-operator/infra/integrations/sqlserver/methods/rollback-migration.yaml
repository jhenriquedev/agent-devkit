id: rollback-migration
description: Execute a rollback migration with --execute confirmation.
inputs:
  required: [path]
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
