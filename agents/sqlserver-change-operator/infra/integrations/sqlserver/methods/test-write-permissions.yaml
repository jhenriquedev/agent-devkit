id: test-write-permissions
description: Test write permission using temporary objects and rollback.
inputs:
  required: []
  optional: [database, execute]
outputs:
  fields: [result]
write_policy: confirm
