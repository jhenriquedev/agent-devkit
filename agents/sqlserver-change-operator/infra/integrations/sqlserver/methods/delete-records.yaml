id: delete-records
description: Delete records using explicit where clause, preview, limit, and confirmation.
inputs:
  required: [schema, table, where]
  optional: [database, execute, confirm_delete, max_affected_rows]
outputs:
  fields: [result]
write_policy: confirm
